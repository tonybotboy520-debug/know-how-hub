# Local development normally leaves this empty and uses the Vite /api proxy.
# Production should point it to the independently deployed Backend service.
VITE_API_BASE_URL=https://your-backend.example.com
